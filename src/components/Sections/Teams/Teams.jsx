import {
  Container,
  Section,
  SectionTitle,
} from 'components/baseStyles/CommonStyle.styled';

export const Teams = () => {
  return (
    <Section>
      <Container>
        <SectionTitle>Teams</SectionTitle>
      </Container>
    </Section>
  );
};
